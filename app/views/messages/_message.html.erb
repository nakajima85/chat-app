<div class="main-center">
  <div class="chat-text">
    <div class="text-user">
      <!-- 投稿したユーザー名情報を出力する -->
      <%= message.user.name %>
    </div>
    <div class="text-date">
      <!-- 投稿した時刻を出力する -->
      <%=  l message.created_at %>
    </div>
  </div>
  <div class="text-post">
    <div class="post-message">
      <!-- 投稿したメッセージ内容を記述する -->
      <%= message.content %>
    </div>
      <%# 
       image_tag message.image, class: 'message-image' ←だけだと画像無でも読み込みエラー
                                                           画像有のみimage_tag読み込み
      <%= image_tag message.image, class: 'message-image' if message.image.attached?  ↓イメージのサイズ指定有%>
      <%= image_tag message.image.variant(resize: '500x500'), class: 'message-image' if message.image.attached? %>

  </div>
</div>